{
  "name": "final",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws": "^0.0.3-2",
    "firebase": "^10.12.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "client-s3-deploy": "aws s3 sync build/ s3://ashley-sotofullstackbankingapplication",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
